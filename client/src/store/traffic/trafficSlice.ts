import { createSlice } from '@reduxjs/toolkit';
import { Dispatch } from 'redux';

const trafficSlice = createSlice({
    name: 'traffic',
    initialState: {

    },
    reducers: {

    }
});

export const { } = trafficSlice.actions;
export default trafficSlice.reducer;
